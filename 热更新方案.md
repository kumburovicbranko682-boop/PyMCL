# PyMCL 热更新方案（EXE 自更新 + GitHub 补丁热更新）

> 状态：**方案设计稿**（未实现）。本文档给出可落地的分阶段方案，实现由后续 PR 分片完成。
> 读者对象：本仓库维护者。所有路径均为仓库内真实文件。

---

## 1. 现状调研（方案的事实基础）

### 1.1 项目与打包形态

- PyMCL 是纯 Python 的 Minecraft 启动器：入口 `main.py`（CLI + GUI），核心包 `mclauncher/`，PySide6 Fluent UI 在 `app/`。
- 发行形态是 **PyInstaller onefile 单文件 EXE**：
  - GUI 版：`PyMCL.spec`（入口 `launcher_entry.py`，`console=False`，`upx=True`，`runtime_tmpdir=None` → 每次启动自解压到系统临时目录）；
  - CLI 版：`PyMCL-CLI.spec`（入口 `main.py`）；
  - 本地打包脚本 `build_exe.bat` / `build_exe.sh`；CI 打包工作流 `.github/workflows/build-windows-exe.yml`（push/tag/PR 触发，只上传 Actions Artifact，**不发 Release**）。
- 语言包等数据文件被打进 EXE 内部（`PyMCL.spec` 里 `datas += [("mclauncher/locales", "mclauncher/locales")]`，`mclauncher/i18n.py::_locales_dir()` 从 `sys._MEIPASS` 读取）——即目前**没有外置资源**。
- 便携模式：`mclauncher/utils.py::_resolve_root()`，数据根目录 `ROOT` = EXE 旁（可写时）或 `%APPDATA%\PyMCL`。**这是热更新落盘的天然位置**。
- 版本号唯一来源：`mclauncher/__init__.py` 的 `APP_VERSION`（当前 `1.0.1`）。
- 仓库另有 `pack/`（WPF/.NET 壳 + C bridge，首次解压 runtime 到 `%LOCALAPPDATA%\PyMCL\runtime\<ver>\`）、`cx_setup.py`、`eziapp/`、`winui3/`、`android/` 等备选前端/打包线，**本方案只针对主线 PyInstaller EXE**（GUI + CLI），其它端不在范围内。

### 1.2 已有的更新相关设施（可直接复用）

| 设施 | 位置 | 现状 |
| --- | --- | --- |
| 整包自更新 | `mclauncher/updater.py` | 已实现：查 GitHub `/releases/latest`（或自定义 `update_url` 清单）→ 强制 SHA-256 门禁 → 下到 `ROOT/cache/` → `apply_exe()` 写 `pymcl-apply-update.bat`，退出后 copy 覆盖并重启 |
| UI 入口 | `app/pages/settings_page.py::_check_update`、`app/backend.py::check_update / start_self_update / _self_update_impl`、`app/main_window.py`（启动时按 `auto_check_update` 后台检查） | 已接好 |
| 下载器 | `mclauncher/downloader.py`（`fetch_json` / `download(url, dest, sha256=...)`，流式校验） | 已实现 |
| GitHub 国内代理 | `mclauncher/mirrors.py::github_candidates()`（ghfast.top 等前缀，`api.github.com` 与资产 URL 均会展开） | 已实现，但**代理是不可信中间人**，见 §6 |
| 崩溃守护 | `mclauncher/guard.py`（全局异常 → `ROOT/pymcl-error.log`）、`launcher_entry.py`（启动失败弹窗） | 可用作补丁回滚的信号源 |
| 签名能力 | `requirements.txt` 已含 `cryptography` | Ed25519 验签**零新增依赖** |

### 1.3 现状的缺口

1. **发布链断裂**：`updater.py` 默认查本仓库 Releases，但仓库**从未发布过 Release**（tag 触发的工作流只传 Artifact），自更新实际永远返回"已是最新"。
2. **只有整包更新**：EXE 约 100+ MB 量级，改一行 Python 也要用户重下整包。
3. **清单无签名**：SHA-256 取自 release 资产 `digest` 或 release 描述，而清单本身可能经 `mirrors.py` 的第三方代理取得——代理可同时篡改清单与包，SHA-256 门禁在这条路径上防不住恶意替换。
4. **替换脚本较脆**：`apply_exe()` 用 `copy /Y` 覆盖，不保留旧版，失败无回滚。

---

## 2. 目标与非目标

**目标**

1. 常规热更新：运行中的 EXE 能发现新版、下载并应用，**尽量少重启**，并明确"必须重启"的边界。
2. GitHub 补丁热更新：改动只涉及纯 Python 代码/数据时，用户只下载 **几十 KB～几 MB 的补丁**（GitHub Release 资产），不必重下整包 EXE。
3. 所有更新通道强制完整性 + 来源校验（SHA-256 + Ed25519 签名），失败可回滚。

**非目标（本期不做）**

- 不改发行形态（保持 onefile 便携 EXE，不引入安装器/onedir）；
- 不做进程内不重启换代码（Qt 应用热替换不可靠，见 §10）；
- 不覆盖 `eziapp` / `winui3` / Android 等实验端。

---

## 3. EXE 场景的硬性边界（哪些能热替换、哪些必须重启）

PyInstaller onefile 的运行模型决定了边界：EXE 内嵌压缩档，启动时解压到临时目录，Python 代码经 `FrozenImporter`（`sys.meta_path` 上的 finder）从档内加载。

| 内容 | 能否不重启热替换 | 说明 |
| --- | --- | --- |
| 外置数据：语言包、镜像/死域名列表、帮助文本、新闻源、AI 网关地址 | ✅ 可以 | 落到 `ROOT/` 下，运行时重读即可（`i18n.py` 已有 `ROOT/locales` 的持久化先例，`mirrors.py` 已支持 config 覆盖 `github_proxy_prefixes`） |
| 纯 Python 代码（`mclauncher/`、`app/`、`main.py`） | ⚠️ 可"下一次启动生效" | 通过代码补丁 overlay（§5），下载后**需重启启动器**装载；不做进程内 `importlib.reload` |
| 第三方库（PySide6/Qt DLL、cryptography 等二进制扩展） | ❌ 不能 | 打在 EXE 档内，只能随整包 EXE 更新 |
| Python 解释器 / PyInstaller bootloader / EXE 自身 | ❌ 不能 | Windows 锁定运行中 EXE 的写入，**但允许改名**——替换方案见 §7.2 |

两个减轻"必须重启"痛感的事实：

- 启动器重启**不影响正在运行的游戏**——Minecraft 是独立子进程，现有"启动后隐藏/关闭启动器"设置（`launcher_visibility`）已依赖这一点；
- 启动器冷启动本身只有几秒，重启成本低。因此方案定位是：**数据不重启、代码一键重启、二进制换整包**。

---

## 4. 总体架构：三条更新通道

```
                GitHub Release (tag vX.Y.Z, CI 自动发布)
                ├── PyMCL.exe / PyMCL-CLI.exe        （通道 C：整包）
                ├── overlay-<base>-p<N>.zip           （通道 B：代码补丁）
                ├── hotdata.json                      （通道 A：热数据）
                └── update.json + update.json.sig     （统一签名清单）
                          │
                          ▼  updater 经 mirrors.github_candidates 下载，Ed25519 验签
        ┌─────────────────┼─────────────────────┐
   通道 A：热数据      通道 B：代码 overlay      通道 C：整包 EXE
   运行时直接生效      落盘 ROOT/patches/       下载新 EXE，
   （无需重启）        提示一键重启生效          退出后 rename 交换 + 重启
```

- **通道 A（热数据，不重启）**：镜像前缀、死域名/死代理名单、公告、帮助文本、语言包增补。体积 KB 级，可高频发。
- **通道 B（代码补丁 overlay，重启生效）**：某个 base 版本（如 `1.0.1`）之后所有纯 Python 改动的**累积包**。改一个 bug 只发几十 KB。这是"GitHub 补丁热更新"的主体。
- **通道 C（整包 EXE，退出替换）**：依赖升级、Python/Qt 升级、spec 改动时使用；也是通道 B 不适用时的兜底。复用并加固现有 `updater.py`。

客户端策略：检查 `update.json` 后自动选择代价最小的通道——`latest` 与本地 `APP_VERSION` 同 base 且只差 patch level → 走 B；base 变了 → 走 C；`hotdata` 版本号变了 → 静默走 A。

---

## 5. 版本、清单与补丁格式

### 5.1 版本表示

- 主版本沿用 `APP_VERSION`（`mclauncher/__init__.py`），随整包 EXE 变化；
- 新增**补丁级** `patch_level`（整数，overlay 携带），界面显示为 `1.0.1+p3`；
- `updater.py::_parse/newer` 需扩展为比较 `(base_version, patch_level)` 二元组。

### 5.2 统一清单 `update.json`（每个 Release 一份资产）

```json
{
  "schema": 1,
  "version": "1.0.2",
  "channel": "stable",
  "notes": "更新说明（沿用 release body）",
  "exe": {
    "url": "https://github.com/<owner>/PyMCL/releases/download/v1.0.2/PyMCL.exe",
    "sha256": "<64hex>",
    "size": 104857600
  },
  "cli": { "url": "...", "sha256": "...", "size": 0 },
  "overlays": [
    {
      "base": "1.0.1",
      "patch_level": 3,
      "url": ".../overlay-1.0.1-p3.zip",
      "sha256": "<64hex>",
      "size": 51200,
      "files": { "mclauncher/updater.py": "<sha256>", "app/pages/settings_page.py": "<sha256>" }
    }
  ],
  "hotdata": { "rev": 17, "url": ".../hotdata.json", "sha256": "<64hex>" },
  "min_supported": "1.0.0"
}
```

- `overlays[]` 允许同一 Release 给**多个旧 base** 提供补丁（如同时给 1.0.1、1.0.2 的用户发补丁）；
- 旁挂分离签名资产 `update.json.sig`（对 `update.json` 原始字节做 Ed25519 签名）；
- 公钥以常量硬编码进 `mclauncher/updater.py`（进 EXE），私钥只存在于 GitHub Actions Secret（占位名 `PYMCL_UPDATE_SIGNING_KEY`，**绝不入库**）。

### 5.3 overlay 补丁包格式

- **累积全量文件包**（不是行级 diff）：`overlay-<base>-p<N>.zip` 包含自 base 版发布以来**所有变更过的纯 Python 源文件**，按仓库相对路径存放（`mclauncher/xxx.py`、`app/xxx.py`、`main.py`、`mclauncher/locales/*.json`）。
  - 选累积全量而非 bsdiff/行级 diff 的原因：① 用户可能跳版本升级，累积包天然幂等，无需链式套补丁；② 纯 py 源文件本身只有 KB～几十 KB，diff 收益可忽略；③ 应用逻辑退化为"解压 + 校验"，无 patch 工具依赖。
- zip 内附 `overlay.json`：`{"base": "1.0.1", "patch_level": 3, "files": {...同清单...}}`，解压后逐文件比对 SHA-256。
- **准入规则**（CI 强制）：`git diff --name-only v<base>..HEAD` 若触碰 `requirements*.txt`、`*.spec`、`launcher_entry.py`、非 `.py/.json` 文件，则该 Release **不产 overlay**，只发整包——保证 overlay 永远不会遇到"需要新依赖/新二进制"的代码。

### 5.4 hotdata 格式

```json
{
  "rev": 17,
  "github_proxy_prefixes": ["https://ghfast.top/", "..."],
  "dead_proxy_prefixes": ["https://gitproxy.mrhjx.cn/"],
  "dead_update_hosts": ["pymcl.dev"],
  "announcement": {"id": "2026-08-x", "text": "...", "level": "info"},
  "locales_patch": {"zh_CN": {"key": "新文案"}}
}
```

对应现有硬编码：`mirrors.py` 的 `GITHUB_PROXY_PREFIXES/DEAD_PROXY_PREFIXES`、`updater.py` 的 `LEGACY_DEAD_HOSTS`。hotdata 验签通过后写 `ROOT/cache/hotdata.json`，各模块读取时"本地 hotdata 覆盖 > config 用户配置 > 内置默认"。语言包增补走已有的 `i18n.add_translations()` 运行时接口。

---

## 6. 安全设计

自动更新是**远程代码执行边界**，且本项目国内用户大量流量经第三方 GitHub 代理（`mirrors.py`），代理必须按不可信中间人对待：

1. **Ed25519 签名强制**：`update.json`、hotdata、overlay（其哈希被签名清单覆盖）未验签一律拒绝，无论从官方直连还是代理取得。用 `cryptography` 实现，无新依赖。整包 EXE 的 SHA-256 写在已验签清单里，从而闭环。
2. **防降级/防重放**：客户端拒绝 `(version, patch_level)` 不大于本地的清单；清单含 `min_supported`，过老客户端提示手动整包升级。
3. **overlay 路径防穿越**：解压时拒绝绝对路径、`..`、盘符、符号链接；只允许落在 `ROOT/patches/<ver>/` 白名单前缀（`mclauncher/`、`app/`、`main.py`）。
4. **保留现有 SHA-256 门禁语义**：`updater.py::check()` 现在对缺失 SHA-256 的清单"只提示不下载"，升级后对缺失**签名**的清单同样只提示、给出手动下载链接。
5. **密钥管理（占位说明）**：私钥仅存 GitHub Actions Secret；文档与代码只出现公钥。预留**双公钥列表**以支持密钥轮换（新旧公钥并存一个版本周期）。
6. **用户同意**：通道 C 与通道 B 默认弹确认（沿用设置页交互）；只有通道 A（纯数据）允许静默应用，且不含任何可执行内容。

---

## 7. 客户端流程与回滚

### 7.1 检查与选择通道

`updater.check()` 扩展（保持现有函数签名与返回 dict 结构，UI 无需大改）：

1. 取 `/releases/latest` → 找 `update.json(.sig)` 资产 → 下载并验签（经 `github_candidates` 多源尝试，失败回退直连，兼容保留现读 `digest` 的旧逻辑作降级提示）；
2. 对比 `(APP_VERSION, 本地 patch_level)`：
   - hotdata `rev` 更新 → 后台静默应用（通道 A）；
   - `overlays[]` 中存在 `base == APP_VERSION` 且 `patch_level >` 本地 → 推荐通道 B；
   - `version` base 大于本地 → 推荐通道 C；
3. 返回结构在现有字段上追加 `"kind": "hotdata"|"overlay"|"exe"`。

### 7.2 应用：通道 C（整包，加固现有 `apply_exe`）

现有 bat 用 `copy /Y` 直接覆盖。改为**rename 交换**（Windows 允许重命名运行中的 EXE）：

```
1. 校验新包（SHA-256，取自已验签清单）→ 暂存 ROOT/cache/PyMCL-<ver>.new
2. 写 pymcl-apply-update.bat：等待旧进程退出（轮询 + 超时）
   → ren PyMCL.exe PyMCL.exe.old
   → move PyMCL-<ver>.new PyMCL.exe
   → start PyMCL.exe --post-update=<ver>
3. 新进程带 --post-update 启动成功（GUI 起来）后删除 .old；
   若 bat 中 move 失败 → ren 回滚 .old → 启动旧版并留错误日志
```

保留 `.old` 直到新版确认自检通过，即得到一键回滚点。EXE 放在 Program Files 等不可写目录时（`_resolve_root()` 已能识别此情形），禁用通道 C 自动替换，仅提示手动下载。

### 7.3 应用：通道 B（overlay，重启生效）

落盘布局（全部在 `ROOT` 下，天然随便携目录走）：

```
ROOT/patches/
├── state.json          # {"active": "1.0.1-p3", "last_good": "1.0.1-p2", "boot_attempts": 0}
├── 1.0.1-p2/ ...
└── 1.0.1-p3/
    ├── overlay.json
    ├── mclauncher/updater.py
    └── app/pages/settings_page.py
```

装载点在 `launcher_entry.py`（以及 `main.py` 供 CLI 版），**在 import `main` 之前**：

1. 读 `state.json`，校验 `active` overlay 的 `base == APP_VERSION`（EXE 整包升级后旧 overlay 自动失效并清理）；
2. 逐文件复核 SHA-256（防落盘后被篡改），任一不符 → 整个 overlay 弃用；
3. 注册一个自定义 `MetaPathFinder` 插到 `sys.meta_path[0]`（PyInstaller 的 `FrozenImporter` 之前），命中 overlay 内模块时从磁盘加载，否则透传冻结档——这是对现有代码**侵入最小**的覆盖方式，`app/`、`mclauncher/` 源码零改动；
4. `boot_attempts += 1` 写盘；GUI 主窗口成功显示后归零并把 `active` 记为 `last_good`。

**安全模式回滚**：启动早期发现 `boot_attempts >= 2`（连续两次未活到主窗口，配合 `guard.py` 的崩溃日志），自动把 `active` 回退到 `last_good`（再失败则完全禁用 overlay 回到 EXE 内置代码），并在下次启动弹条说明。用户也可在设置页手动"禁用补丁"。

UI 交互：设置页"检查更新"发现 overlay → 显示 patch 说明与体积 → 用户确认 → 后台下载校验 → InfoBar 提示"补丁已就绪，重启启动器生效【立即重启】"。立即重启 = `os.execl` 自身（游戏进程不受影响，见 §3）。

### 7.4 应用：通道 A（hotdata）

启动后台检查时顺带拉取（复用现有 `auto_check_update` 开关与 `main_window.py` 的静默检查点），验签后写 `ROOT/cache/hotdata.json` 并立即调用各模块的刷新入口（镜像列表、公告、`i18n.add_translations`）。失败静默，绝不打扰用户。

---

## 8. 与构建/发布流程的衔接（CI 改动清单）

新增 `.github/workflows/release.yml`（`build-windows-exe.yml` 保持不动，继续做 PR/push 冒烟构建）：

```
触发：push tag v*
1. 校验 tag 版本 == mclauncher/__init__.py 的 APP_VERSION（不一致直接 fail）
2. 复用现有构建步骤产出 dist/PyMCL.exe、dist/PyMCL-CLI.exe
3. 产 overlay：对最近 N 个已发布 base 版本分别执行准入检查（§5.3），
   通过者打 overlay-<base>-p<N>.zip（patch_level = 该 base 已发补丁数 + 1）
4. 生成 hotdata.json（仓库内维护源文件，如 pack/hotdata.src.json，CI 只做校验+签名）
5. 汇总生成 update.json；用 secrets.PYMCL_UPDATE_SIGNING_KEY 产 update.json.sig
6. gh release create v<ver> 上传全部资产；release body 保留 SHA-256 清单
   （兼容现有 updater.py 从 body 提取哈希的降级路径）
```

补丁快速通道（不发整包）：打 `v1.0.1-p3` 这类 tag → 工作流跳过 EXE 构建，只产 overlay + 更新清单，Release 标记为 pre-release 或复用"滚动 patch Release"。这使**修一个 bug 从提交到用户可更新只需一次 tag push**，无需等 45 分钟的 Windows 构建。

仓库内需要动的文件（实现阶段）：

| 文件 | 改动 |
| --- | --- |
| `mclauncher/updater.py` | 清单 schema 升级、Ed25519 验签、通道选择、rename 交换 bat、overlay 下载与 state 管理 |
| `launcher_entry.py` / `main.py` | 启动早期装载 overlay 的 MetaPathFinder + 安全模式（约几十行，唯一的"骨架"性改动） |
| `mclauncher/mirrors.py` | 读取 hotdata 覆盖前缀/死名单 |
| `app/backend.py`、`app/pages/settings_page.py` | 更新结果的 `kind` 分支、补丁确认/一键重启/禁用补丁入口 |
| `.github/workflows/release.yml`（新增）+ 发布辅助脚本（建议放 `pack/release_tools.py`） | 见上 |
| `tests/` | overlay 装载、验签、防穿越、版本比较、安全模式的单测（仓库已有 `tests/` 与 `test_crash.py` 风格可循） |

---

## 9. 分阶段落地

| 阶段 | 内容 | 交付判据 |
| --- | --- | --- |
| **P0 打通发布链** | 新增 `release.yml`：tag → 构建 → 发 Release（EXE + body 哈希）。不改客户端 | 打 tag 后现有 `updater.py` 能真实发现并完成整包更新 |
| **P1 加固整包通道** | `update.json(.sig)` + Ed25519 验签；`apply_exe` 改 rename 交换 + `.old` 回滚 + `--post-update` 自检 | 断网/篡改/替换失败三类故障注入全部安全收敛 |
| **P2 热数据通道** | hotdata 生成、验签、镜像/公告/语言包运行时刷新 | 不发版即可下线一个失效代理前缀 |
| **P3 overlay 补丁通道** | CI 产 overlay + 客户端 MetaPathFinder 装载 + 安全模式回滚 + 设置页交互 | 一个只改 py 的 bugfix，用户以 <100KB 下载量 + 一次重启完成升级 |
| **P4 可选优化** | 补丁快速通道 tag（`v*-p*`）、多 base overlay、密钥轮换演练 | 按需 |

P0/P1 独立可用；P2、P3 相互独立，可并行。每阶段单独 PR。

---

## 10. 推荐、备选与不建议

**推荐（即本文主体）**：保持 onefile 便携 EXE 不变，三通道分层——数据热更（不重启）、签名 overlay 补丁（一键重启）、整包 rename 交换（退出替换）。理由：贴合现有 `updater.py`/`downloader.py`/`guard.py` 资产，纯 Python 项目做源级 overlay 是天然优势，改动集中在 updater + 入口引导，风险面小。

**备选 A：瘦壳 + 外置运行时**。EXE 退化为纯引导壳，首启把 Python 运行时与全部代码解压到 `%LOCALAPPDATA%\PyMCL\runtime\<ver>\`（`pack/` 的 WPF 线已有此结构先例），更新时按 manifest 逐文件差量替换。粒度最好、整包更新也只下变化的 DLL，但等于重做发行形态、动摇"单文件便携"卖点，作为**长期演进方向**保留。

**备选 B：EXE 二进制差量（bsdiff/HDiffPatch）**。对 onefile 收益差：PyInstaller 内嵌档是逐文件 zlib 压缩，叠加 `upx=True`，源码一行改动会雪崩式改变二进制布局，实测这类包 diff 率常达 60%+ 原体积。若未来确需做，前提是构建改为不压缩归档 + 关 UPX（体积代价约 +30%），且要自带 patch 工具。**不作为近期选项**。

**不建议做**：

1. **进程内不重启热换代码**（`importlib.reload` / 猴子补丁换 `app/` 模块）：PySide6 的对象树、信号连接、单例 backend 无法安全重建，收益（省一次几秒的重启）远低于稳定性风险；
2. **无签名的补丁/清单**：等于把 RCE 入口交给任何一个 GitHub 代理节点；同理**不要**用"从 release 正文提取哈希"作为唯一校验（现有降级路径只可用于提示，不可用于自动安装）；
3. **overlay 携带非 Python 内容**（DLL/pyd/新依赖）：版本漂移无法收敛，准入规则（§5.3）必须由 CI 强制；
4. **绕过用户确认静默替换 EXE 或代码**：数据通道以外的静默更新一律禁止；
5. **链式增量补丁**（p1→p2→p3 逐级套用）：跳版本用户的失败面太大，坚持累积全量 overlay。

---

## 11. 主要风险

| 风险 | 缓解 |
| --- | --- |
| 私钥泄漏 → 恶意更新 | 密钥只存 Actions Secret；双公钥轮换预案；清单含版本单调性防降级 |
| overlay 与 EXE 内置代码不一致导致隐性 bug | overlay 强绑 `base == APP_VERSION`；CI 准入检查；`files` 逐文件哈希复核 |
| 补丁把启动器改挂 | `boot_attempts` 安全模式自动回退 + 设置页手动禁用 + `guard.py` 日志留证 |
| 杀软误报替换脚本/新 EXE | rename 交换比自写入更温和；文档保留 onedir 建议；Release 页始终提供手动下载兜底 |
| GitHub 直连不可达 | 复用 `mirrors.py` 多代理候选；一切内容过验签后代理不可信也无害 |
| EXE 在只读目录 | 沿用 `_resolve_root()` 的探测：禁自动替换、只提示手动更新；overlay/数据仍可用（落 `%APPDATA%`） |
